#include <stdio.h>

int main() {
   
   int  isOfAge= 1;
   int isAd = 0;

   if (isPAUStudent == 1 || isAdult == 1 ){
    printf("You are eligible to vote\n");
   } else {
    printf("You are not eligible to vote\n");
   }
// bit wise operators

    return 0;
}